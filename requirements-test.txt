# Test-only dependencies. Kept separate from requirements.txt so the
# production Railway image stays lean. Install locally with:
#   pip install -r requirements-test.txt
#
# Run the suite with:
#   pytest -q

pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0  # AsyncClient for FastAPI endpoint tests
